@model IEnumerable<Domain.Entities.ProductCategory>
@{
    Layout = null;  
}
@Html.ActionLink("Home","List","Product")
@foreach(var item in Model)
{
    @Html.RouteLink(
            item.Name, 
            new {controller = "Product", action = "List", category = item.Name, page=1}
        );

}